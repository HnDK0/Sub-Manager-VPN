# Build artifacts (the linux binary is built by CI and published as a GitHub
# Release — never committed into the repo)
submanager-linux
sub-manager
vpn-sub-manager
*.exe

# Downloaded assets (GeoIP mmdb etc., fetched at setup, not committed)
assets/*.mmdb

# Runtime state & generated data (contains secrets / local state — never commit)
state.db
*.db
out/
cores/
bans.json
config.json
sources.txt

# OS / editor noise
.DS_Store
.idea/
.vscode/
